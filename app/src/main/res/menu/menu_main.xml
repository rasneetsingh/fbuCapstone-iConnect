<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_logout"
        android:title="Logout" />

    <item android:id="@+id/settings"
        android:title="Settings" />

    <item android:id="@+id/action_search"
        android:title="Search"
        android:icon="@android:drawable/ic_menu_search"
        app:showAsAction="always"
        app:actionViewClass= "android.widget.SearchView" />

    <item android:id="@+id/action_add"
        android:title="Add Post"
        android:icon="@drawable/ic_add3"
        app:showAsAction="always"/>

    <item android:id="@+id/action_create_group"
        android:title="Create Group"
        android:icon="@drawable/ic_groups"
        app:showAsAction="always"/>

    <item android:id="@+id/action_add_participant"
        android:title="Add Participant"
        android:icon="@drawable/ic_personadd"
        app:showAsAction="always"/>







        />

</menu>